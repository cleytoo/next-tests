describe("MakeValidatedTodo (unit)", () => {
  it("should be able to call sanitizeStr with the correct value", () => {});

  it("should be able to call validateAllDescription with the return of sanitizeStr", () => {});

  it("should be able to call makeNewTodo if validatedDescription returns success", () => {});

  it("should return a list of errors if validatedDescription fails", () => {});
});
